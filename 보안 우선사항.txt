7) 수정 예시: 

app/api/events/create/route.ts
app/api/qr/create/route.ts

패턴은 딱 하나입니다.

Request → NextRequest
assertSameOrigin(request) 추가



10) CSRF 적용 대상 라우트

아래 파일들에는 전부 같은 방식으로 넣는 게 좋습니다.

import { NextRequest } from 'next/server'
import { assertSameOrigin } from '@/lib/security/csrf'
import { jsonNoStore } from '@/lib/security/api-response'

export async function POST(request: NextRequest) {
  try {
    assertSameOrigin(request)

    // 기존 로직
  } catch (error) {
    if (error instanceof Error && error.message === 'CSRF_BLOCKED') {
      return jsonNoStore({ error: '허용되지 않은 요청입니다.' }, { status: 403 })
    }

    return jsonNoStore({ error: '서버 오류가 발생했습니다.' }, { status: 500 })
  }
}

app/api/auth/logout/route.ts        -o
app/api/events/create/route.ts      -0
app/api/events/update/route.ts      -0
app/api/events/delete/route.ts      -0
app/api/qr/create/route.ts          -0
app/api/qr/delete/route.ts          -0
app/api/qr/list/route.ts            -0  
app/api/attendance/check/route.ts   -0  
app/api/attendance/edit/route.ts    -0  
app/api/attendance/detail/route.ts  -0  
app/api/attendance/delete/route.ts   
app/api/attendance/list/route.ts    -0
app/api/logs/route.ts               -0  
app/api/profiles/list/route.ts      -0




11) 최소 체크리스트

적용 후 확인할 것:

# 운영 필수
NEXT_PUBLIC_APP_URL=https://your-domain.com
APP_URL=https://your-domain.com
UPSTASH_REDIS_REST_URL=...
UPSTASH_REDIS_REST_TOKEN=...
12) 한 줄 결론

지금 단계에서 가장 효과 큰 패치는 이 3개입니다.

모든 POST API에 assertSameOrigin(request)
로그인 API에 IP/계정 기준 rate limit
next.config.mjs로 보안 헤더/CSP 전역 적용



-----------
2) 로그인 brute-force 방어 없음 — 높음

영향:

/api/auth/login에 무차별 대입 공격 가능
학번 기반 이메일 구조라 계정 ID 예측도 쉬움 (studentId@club.local)

근거:

app/api/auth/login/route.ts:21-106 에 rate limit, lockout, cooldown 없음

권장:

IP + student_id 기준 rate limit
실패 횟수 누적 잠금
짧은 시간 다중 실패 시 429
가능하면 CAPTCHA는 관리자/공용망 환경에서만 선택 적용